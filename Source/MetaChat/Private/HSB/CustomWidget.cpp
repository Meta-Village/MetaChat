#include "HSB/CustomWidget.h"
#include "HSB/InvSlotWidget.h"

void UCustomWidget::OnItemClicked(UObject* Item)
{
    // Cast를 사용하지 않고, 다른 방법으로 FInventoryItem 데이터를 얻는 로직을 구현해야 합니다.
    // 예를 들어, 아이템이 UObject 기반이 아니라면 이 함수 자체가 잘못된 형태일 수 있습니다.
   

}
